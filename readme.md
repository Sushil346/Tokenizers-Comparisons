# Tokenizers-Comparison
I deep dived into tokenizers: comparing different tokenization methods and config files & their architectures.

and built an interactive dashboard using streamlit to visualize how different architectures (GPT, BERT, T5) perceive the same sentence.